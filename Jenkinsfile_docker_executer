pipeline {
    agent any
    triggers {
        pollSCM('*/1 * * * *')
    }
    stages {
        stage('exe') {
            steps {
                sh "docker build -t api_test_executer . -f Dockerfile_executer"
            }
        }
    }
}